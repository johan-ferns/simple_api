# Copy this to .env and adjust values

# Environment
ENVIRONMENT=production

# Server
HOST=0.0.0.0
PORT=8000

# Model settings (for future ML models)
# MODEL_PATH=/path/to/your/model
# MODEL_DEVICE=cpu